# Changelog

## [Unreleased]

## [0.2.4] - 2026-08-22

### Fixed

- `updated` フィールドだけが同期された場合に再度タイムスタンプを書き込まないようにし、複数端末間の同期ループを防止した。

## [0.2.3] - 2026-08-22

### Changed

- Canvas上のカード複製を、ファイル複製ではなく同じカードファイルを参照する配置ノードの追加へ変更した。
- カード配置時の完了通知を表示しないようにした。

## [0.2.2] - 2026-08-22

### Added

- Canvasファイルが1件もない場合、Canvas選択画面から「新規キャンバスを作成」を実行できるようにした。
- 新規Canvas作成後、選択したVisual Cardのテキスト面または手書き面を続けて追加できるようにした。

### Fixed

- 空のCanvasが`{}`として保存されていても、`nodes`と`edges`を補完してカードを追加できるようにした。
- `nodes`または`edges`が未定義のCanvasを、追加処理前に不正形式として扱わないようにした。

### Verification

- `npm run build`
- `npx tsc --noEmit`
- `node --check main.js`
